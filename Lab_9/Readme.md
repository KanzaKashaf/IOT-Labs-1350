# Lab9 - Thinkspeak 

## Class Tasks

### Task 1:

The data from sensor is sending to thinkspeak (cloud) using url with write api key. With delay of 20 sec, as free thinkspeak share data after 15 sec.

---

### Task 2:

The data from sensor is sending to thinkspeak with write api key. On thinkspeak, using react we write a code of Matlab Analysis where we defined threashold for alert. Data is sending through url
and then check threashold value and using read api key we return its result on oled if alert exist.

---