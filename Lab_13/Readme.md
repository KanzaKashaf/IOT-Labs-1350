# Lab13 - Influx DB

## Class Work

### Task 1:

The data from the sensor published to mqtt broker (mosquitto) through port 1883 and check it on power shell is data is publishing susscessfully. We create a client object then set it's server and then by using client publish data to mqtt broker according to topic

Mqtt broker:
Subscribe data from one side and publish data to another side
It has topic according to which it subscribe and publish data
Payload contains the actual msg

---

### Task 2:

The data from sensor published to mqtt broker (mosquitto) through port 1883 and then subscribed into python and then that data is published on influx db by creating a point object

---

### Task 3:

Generating senthetic data with noise and then training a neural network for classification of 5 classes and then gererate report and confusion matrix

---

### Task 4:

The data is published to mqqtt broker (mosquitto) through port 1883 and then subscribe into python and pass data through the trained model as input it will predict class label and then published that data on the infludb by creating point object

localhost:8086 ---> InfluxDB

---